# build config
skipDoc = true
ignoreFailedTest = true
# Project config
version=1.0.0-SNAPSHOT
group=de.braintags
artifactid=vertx-auth-datastore
projectDescription=authentication based on datastore from pojo-mapper

# dependencies
vertxVersion = 3.3.2
btVertxVersion = 1.3.2-SNAPSHOT
